# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
.env.*

# skill/data staging dir (created transiently by update_index.py)
skill/data/.new/

# Pipeline intermediates — not needed at runtime
data/raw/
data/embeddings.npy
data/unified_skills.jsonl
data/unified_skills_ordered.jsonl
data/filter_cache.jsonl
data/trial/

# Incremental crawl state (machine-local; cached by CI, not committed)
data/crawl_state/
data/deep_crawl_state.json

# Local logs
logs/

# Test / coverage artefacts
.pytest_cache/
.coverage
htmlcov/

# Claude Code local settings (may contain tokens / allowlists)
.claude/
