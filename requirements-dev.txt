# Runtime deps (also needed for tests)
numpy>=1.26
faiss-cpu>=1.8
requests>=2.31
pyyaml>=6.0

# Test framework
pytest>=8.0
pytest-cov>=5.0

# Crawler deps (SkillHub scraper)
beautifulsoup4>=4.12
lxml>=5.0

# No sentence-transformers, no openai — removed from scope
